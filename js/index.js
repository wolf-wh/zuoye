window.onload=function(){    let tables = document.querySelector('tbody');    let adds = document.querySelector('button');    tables.addEventListener ('dblclick',fn);    function fn(e){        let element = e.target;        if (element.nodeName == 'TD' && element.className != 'del'){            let old = element.innerText;            element.innerText = '';            let inputs = document.createElement('input');            inputs.style.cssText='width:70px;'            inputs.value = old;            inputs.setAttribute("autofocus", "autofocus");            inputs.focus();            element.appendChild(inputs);            inputs.onblur = function(){                let news = inputs.value;                element.removeChild(inputs);                element.innerText = news;            }        }else if(element.className == 'del'){            let dels = element.parentNode;            tables.removeChild(dels);        }    }    adds.addEventListener('dblclick',fns);    function fns(){        let newli = document.createElement('tr');        newli.innerHTML= `			<td></td>			<td></td>			<td></td>			<td></td>			<td></td>			<td class="del">删除</td>        `        tables.appendChild(newli);    }}